{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd shared && npm install && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "cd shared && npm install && cd ../client && npm install"
}